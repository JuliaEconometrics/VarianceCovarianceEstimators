# Documentation: http://docs.travis-ci.com/user/languages/julia/
language: julia
os:
  - linux
julia:
  - 0.7.0-DEV.2279
notifications:
  email: false
# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia -e 'Pkg.clone(pwd()); Pkg.build("VarianceCovarianceEstimators"); Pkg.test("VarianceCovarianceEstimators"; coverage=true)'
after_success:
  # push coverage results to Codecov
  - julia -e 'cd(Pkg.dir("VarianceCovarianceEstimators")); Pkg.add("Coverage"); using Coverage; Codecov.submit(Codecov.process_folder())'
  # push documentation
  - julia -e 'Pkg.add("Documenter")'
  - julia -e 'Pkg.add("DataFrames")'
  - julia -e 'Pkg.clone("https://github.com/JuliaEconometrics/EconUtils.jl")'
  - julia -e 'cd(Pkg.dir("VarianceCovarianceEstimators")); include(joinpath("docs", "make.jl"))'
